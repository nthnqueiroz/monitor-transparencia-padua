# Node / Next.js (painel em dashboard/)
node_modules/
.next/
out/
build/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Cópias derivadas — geradas por `npm run sync-data` / `npm run sync-conteudo`.
# Os originais vivem na raiz do repo e em textos/, e são versionados lá.
dashboard/public/inventory.csv
dashboard/public/conteudo.json

# Ambiente
.env
.env.local
.env*.local

# Python (monitor + textos/)
__pycache__/
*.pyc
.venv/
venv/

# PDFs baixados pelo pipeline de extração — cache regenerável, não versiona.
# textos/conteudo.json e textos/estado.json (o índice e o ledger) SÃO
# versionados: são pequenos e caros de refazer (dependem de rede).
textos/pdfs/

# Sistema
.DS_Store
Thumbs.db
*.log
